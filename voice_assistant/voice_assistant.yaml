common:
    room_name:
    assistant_type:
    connection_protocol:

connection:
    mqtt:
        broker_ip:
        broker_port:
        client_id:
        username:
        password:
        mqtt_version:

assistant:
    google:
        device_model:
        
    snips:
        placeholder: